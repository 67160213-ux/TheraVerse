# Copy to .env for local `npm run dev`. When running the whole stack via the
# root docker-compose.yml, this is instead passed as a build arg — see
# ../docker-compose.yml (VITE_API_URL).

VITE_API_URL=http://localhost:4000/api
