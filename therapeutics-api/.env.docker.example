# Copy to .env in the project root — docker-compose reads this automatically.

POSTGRES_USER=therapeutics
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_DB=therapeutics
POSTGRES_PORT=5432

API_PORT=4000
CORS_ORIGIN=http://localhost:5173

ADMINER_PORT=8080
